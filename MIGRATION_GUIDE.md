# Migration Guide - localStorage to MySQL

## Обзор

Это руководство поможет перенести существующие заявки из localStorage в MySQL базу данных.

## Зачем нужна миграция?

**Старая система:**
- ✗ Заявки хранятся только в браузере разработчика
- ✗ Другие пользователи не видят заявки
- ✗ В инкогнито режиме заявки не сохраняются
- ✗ При очистке кэша - все теряется

**Новая система:**
- ✅ Заявки хранятся в MySQL БД на сервере
- ✅ Все пользователи отправляют в одну БД
- ✅ Работает в инкогнито режиме
- ✅ Данные не теряются при очистке кэша
- ✅ Доступны из любого браузера/устройства

## Автоматическая миграция

### Способ 1: Через консоль браузера

Если у вас есть заявки в localStorage на локальном компьютере:

1. Откройте сайт в браузере где есть данные
2. Нажмите F12 (Developer Tools)
3. Перейдите на вкладку **Console**
4. Выполните скрипт:

```javascript
// Получаем все заявки из localStorage
const orders = JSON.parse(localStorage.getItem('orders') || '[]');

console.log(`📦 Найдено заявок: ${orders.length}`);

// Функция для отправки одной заявки на сервер
async function migrateOrder(order) {
    try {
        const response = await fetch('api/submit-form.php', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json'
            },
            body: JSON.stringify(order)
        });
        
        const result = await response.json();
        
        if (result.success) {
            console.log(`✅ Заявка ${order.orderNumber} перенесена`);
            return true;
        } else {
            console.error(`❌ Ошибка: ${result.error}`);
            return false;
        }
    } catch (error) {
        console.error(`❌ Ошибка отправки:`, error);
        return false;
    }
}

// Переносим все заявки по очереди
async function migrateAll() {
    let success = 0;
    let failed = 0;
    
    for (let i = 0; i < orders.length; i++) {
        const order = orders[i];
        console.log(`🔄 Переношу ${i + 1}/${orders.length}...`);
        
        const result = await migrateOrder(order);
        
        if (result) {
            success++;
        } else {
            failed++;
        }
        
        // Пауза между запросами
        await new Promise(resolve => setTimeout(resolve, 500));
    }
    
    console.log(`\n✅ Перенесено успешно: ${success}`);
    console.log(`❌ Ошибок: ${failed}`);
    console.log(`\n💡 Проверьте заявки в PHPMyAdmin или админ панели`);
}

// Запускаем миграцию
migrateAll();
```

5. Дождитесь завершения миграции
6. Проверьте результаты в консоли
7. Откройте PHPMyAdmin и проверьте таблицу `orders`

### Способ 2: Экспорт/Импорт через JSON

#### Экспорт из localStorage:

1. Откройте сайт где есть данные
2. Откройте консоль (F12)
3. Выполните:

```javascript
const orders = JSON.parse(localStorage.getItem('orders') || '[]');
console.log(JSON.stringify(orders, null, 2));
```

4. Скопируйте весь JSON из консоли
5. Сохраните в файл `orders_backup.json`

#### Импорт в MySQL:

**Вариант А: Через PHP скрипт**

Создайте файл `api/import-orders.php`:

```php
<?php
require_once 'config.php';

// Только для локального использования!
// Удалите этот файл после импорта!

$json = file_get_contents('orders_backup.json');
$orders = json_decode($json, true);

$dsn = "mysql:host=" . DB_HOST . ";dbname=" . DB_NAME . ";charset=" . DB_CHARSET;
$pdo = new PDO($dsn, DB_USER, DB_PASS);

$imported = 0;
$errors = 0;

foreach ($orders as $order) {
    try {
        $sql = "INSERT INTO orders (
            order_number, type, name, email, phone, telegram,
            service, subject, message, amount, calculator_data,
            status, telegram_sent, created_at
        ) VALUES (
            :order_number, :type, :name, :email, :phone, :telegram,
            :service, :subject, :message, :amount, :calculator_data,
            :status, :telegram_sent, :created_at
        )";
        
        $stmt = $pdo->prepare($sql);
        $stmt->execute([
            ':order_number' => $order['orderNumber'] ?? 'ORD-MIGRATED-' . time() . '-' . $imported,
            ':type' => $order['type'] ?? 'contact',
            ':name' => $order['name'] ?? $order['clientName'] ?? 'Unknown',
            ':email' => $order['email'] ?? $order['clientEmail'] ?? null,
            ':phone' => $order['phone'] ?? $order['clientPhone'] ?? '',
            ':telegram' => $order['telegram'] ?? null,
            ':service' => $order['service'] ?? 'Обращение',
            ':subject' => $order['subject'] ?? null,
            ':message' => $order['message'] ?? $order['details'] ?? null,
            ':amount' => $order['amount'] ?? 0,
            ':calculator_data' => isset($order['calculatorData']) ? json_encode($order['calculatorData']) : null,
            ':status' => $order['status'] ?? 'new',
            ':telegram_sent' => $order['telegramSent'] ?? false,
            ':created_at' => $order['timestamp'] ?? date('Y-m-d H:i:s')
        ]);
        
        $imported++;
    } catch (Exception $e) {
        $errors++;
        echo "Error: " . $e->getMessage() . "\n";
    }
}

echo "✅ Imported: $imported\n";
echo "❌ Errors: $errors\n";
?>
```

Затем:
1. Загрузите `orders_backup.json` и `import-orders.php` в папку `api/`
2. Откройте в браузере: `https://ваш-домен.ru/api/import-orders.php`
3. Проверьте результат
4. **УДАЛИТЕ оба файла после импорта!**

**Вариант Б: Прямой SQL импорт**

Если у вас мало заявок, можно вручную вставить через PHPMyAdmin:

1. Откройте PHPMyAdmin
2. Выберите таблицу `orders`
3. Нажмите **Insert**
4. Заполните поля из JSON
5. Нажмите **Go**

## Ручная миграция

Если автоматическая миграция не работает:

### Шаг 1: Экспорт данных

1. Откройте сайт в браузере где есть данные
2. Откройте Developer Tools (F12)
3. Перейдите на вкладку **Application** (Chrome) или **Storage** (Firefox)
4. Выберите **Local Storage** → ваш домен
5. Найдите ключ `orders`
6. Скопируйте значение

### Шаг 2: Форматирование

Значение будет в виде JSON. Пример:

```json
[
  {
    "id": "1",
    "orderNumber": "ORD-20250110-ABC123",
    "name": "Иван Петров",
    "phone": "+7 (999) 123-45-67",
    "email": "ivan@example.com",
    "message": "Хочу заказать 3D печать",
    "service": "FDM печать (PLA)",
    "amount": 1500,
    "status": "new",
    "timestamp": "2025-01-10T14:30:00.000Z"
  }
]
```

### Шаг 3: Вставка в БД

Для каждой заявки выполните SQL:

```sql
INSERT INTO orders (
    order_number, name, phone, email, message, service, amount, status, created_at
) VALUES (
    'ORD-20250110-ABC123',
    'Иван Петров',
    '+7 (999) 123-45-67',
    'ivan@example.com',
    'Хочу заказать 3D печать',
    'FDM печать (PLA)',
    1500,
    'new',
    '2025-01-10 14:30:00'
);
```

## Проверка миграции

После миграции проверьте:

### 1. В PHPMyAdmin

1. Откройте PHPMyAdmin
2. Выберите базу `ch167436_3dprint`
3. Откройте таблицу `orders`
4. Нажмите **Browse**
5. Проверьте что все заявки на месте

### 2. В админ панели

1. Откройте админ панель: `https://ваш-домен.ru/admin.html`
2. Войдите под админом
3. Перейдите в раздел **Orders**
4. Проверьте что все заявки отображаются

### 3. Подсчет

```sql
-- Общее количество заявок
SELECT COUNT(*) as total FROM orders;

-- По статусам
SELECT status, COUNT(*) as count FROM orders GROUP BY status;

-- По типам
SELECT type, COUNT(*) as count FROM orders GROUP BY type;

-- Последние 10
SELECT * FROM orders ORDER BY created_at DESC LIMIT 10;
```

## Очистка после миграции

После успешной миграции можно:

### Очистить localStorage (опционально)

**⚠️ ВНИМАНИЕ: Делайте это только после проверки что все данные в БД!**

```javascript
// Создать резервную копию перед очисткой
const backup = localStorage.getItem('orders');
console.log('Backup:', backup);

// Очистить orders из localStorage
localStorage.removeItem('orders');

// Проверить
console.log('Orders in localStorage:', localStorage.getItem('orders')); // должен быть null
```

### Удалить временные файлы

- `api/import-orders.php` (если создавали)
- `orders_backup.json` (если создавали)

## Troubleshooting

### Проблема: "Duplicate entry for key 'order_number'"

**Причина:** Заявка с таким номером уже есть в БД

**Решение:**
- Пропустить эту заявку (уже импортирована)
- Или изменить номер заказа перед импортом

### Проблема: "Unknown column in field list"

**Причина:** Структура таблицы не соответствует данным

**Решение:**
1. Проверьте что выполнен `database/schema.sql`
2. Проверьте названия колонок в таблице `orders`
3. Обновите SQL запрос

### Проблема: "JSON parse error"

**Причина:** Невалидный JSON в localStorage

**Решение:**
1. Проверьте JSON через jsonlint.com
2. Исправьте синтаксис
3. Попробуйте снова

### Проблема: Пустой результат

**Причина:** В localStorage нет заявок

**Решение:**
- Проверьте что открыли правильный сайт
- Проверьте другие браузеры
- Возможно, данные уже были удалены

## Рекомендации

1. **Создайте бэкап перед миграцией**
   ```javascript
   const backup = localStorage.getItem('orders');
   // Сохраните это в файл
   ```

2. **Тестируйте на нескольких заявках**
   - Сначала перенесите 1-2 заявки
   - Проверьте что они корректно отображаются
   - Затем мигрируйте все

3. **Не удаляйте localStorage сразу**
   - Держите резервную копию неделю
   - Убедитесь что новая система работает
   - Затем удаляйте

4. **Документируйте процесс**
   - Записывайте что делали
   - Сохраняйте скриншоты
   - Фиксируйте ошибки

## Итог

После успешной миграции:

✅ Все старые заявки перенесены в MySQL  
✅ Новые заявки автоматически сохраняются в БД  
✅ Работает в инкогнито режиме  
✅ Работает для всех пользователей  
✅ Данные не теряются  

Теперь система полностью готова к работе! 🎉
