# 📱 Telegram Bot Setup Guide

## Проблема: Заявки не приходят на хостинге

Если заявки с сайта не приходят в Telegram, это означает что **Telegram Bot не настроен правильно**.

---

## ✅ Решение: Настройка Telegram Bot (5 минут)

### Шаг 1: Создание бота (если еще не создан)

1. Откройте Telegram и найдите бота **@BotFather**
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "3D Print Pro Bot")
   - Введите username бота (например: "printpro_omsk_bot")
4. **BotFather вернет вам Bot Token** - сохраните его!
   - Пример: `8241807858:AAE0JXxWO9HumqesNK6x_vvaMrxvRK9qKBI`

### Шаг 2: Получение Chat ID

1. Напишите своему боту любое сообщение (например, "Привет")
2. Откройте в браузере:
   ```
   https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
   ```
   Замените `<YOUR_BOT_TOKEN>` на ваш токен
   
3. В ответе найдите `"chat":{"id":123456789}`
4. **Сохраните это число** - это ваш Chat ID

### Шаг 3: Настройка в админ-панели

1. Откройте сайт и перейдите на `/admin.html`
2. Войдите в админ-панель (логин по умолчанию: `admin` / `admin123`)
3. Перейдите в раздел **"Настройки"** → вкладка **"Telegram"**
4. Введите:
   - **Bot Token** (из Шага 1)
   - **Chat ID** (из Шага 2)
5. Нажмите **"Сохранить настройки"**
6. Нажмите **"Отправить тестовое сообщение"** для проверки

### Шаг 4: Проверка

1. Отправьте тестовую заявку с сайта (используйте калькулятор или контактную форму)
2. Сообщение должно прийти в Telegram
3. Проверьте консоль браузера (F12) - должны быть логи:
   ```
   ✅ Telegram уведомление отправлено успешно!
   ```

---

## 🔍 Диагностика проблем

### Проблема: "Chat ID не настроен"

**Симптомы:**
- В консоли: `❌ ОШИБКА: Telegram Chat ID не настроен!`
- Пользователь видит: `⚠️ Telegram не настроен`

**Решение:**
1. Откройте `/admin.html` → Настройки → Telegram
2. Получите Chat ID (см. Шаг 2 выше)
3. Сохраните Chat ID в настройках
4. Отправьте тестовое сообщение

### Проблема: "Неверный Bot Token"

**Симптомы:**
- В консоли: `❌ Telegram API вернул ошибку: 401 Unauthorized`
- Сообщение: `Неверный Bot Token`

**Решение:**
1. Проверьте правильность Bot Token в `/admin.html`
2. Token должен быть в формате: `12345:ABCDEFG...`
3. Убедитесь что нет пробелов в начале/конце
4. Получите новый токен от @BotFather если нужно

### Проблема: "Неверный Chat ID"

**Симптомы:**
- В консоли: `❌ Telegram API вернул ошибку: 400 Bad Request`
- Сообщение: `Chat not found`

**Решение:**
1. Напишите боту сообщение (обязательно!)
2. Получите Chat ID через `getUpdates` API
3. Обновите Chat ID в настройках
4. Chat ID должен быть числом (например: `123456789`)

### Проблема: CORS / Network Error

**Симптомы:**
- В консоли: `Failed to fetch` или CORS error
- Сообщение: `Ошибка соединения с Telegram`

**Решение:**
- Telegram API работает через HTTPS и не блокируется CORS
- Проверьте интернет-соединение
- Убедитесь что хостинг поддерживает исходящие HTTPS запросы
- Попробуйте открыть в браузере: `https://api.telegram.org/` (должна быть страница)

---

## 📋 Checklist для production

Перед публикацией на хостинг проверьте:

- [ ] Bot Token правильно настроен в `/admin.html`
- [ ] Chat ID правильно настроен в `/admin.html`
- [ ] Отправлено тестовое сообщение успешно
- [ ] Отправлена тестовая заявка с калькулятора
- [ ] Отправлена тестовая заявка из контактной формы
- [ ] В консоли нет ошибок Telegram
- [ ] В админ-панели заказы сохраняются с `telegramSent: true`

---

## 🔒 Безопасность

**ВАЖНО:** 
- Bot Token хранится в `config.js` на клиенте (это нормально для статических сайтов)
- Сообщения может отправлять только владелец токена
- Никто не может читать сообщения без доступа к боту
- Если токен утек - создайте нового бота через @BotFather

**Рекомендации:**
- Измените пароль админ-панели (по умолчанию `admin123`)
- Не публикуйте Bot Token в открытых репозиториях
- Регулярно проверяйте историю сообщений бота

---

## 🆘 Поддержка

Если проблема не решается:

1. **Проверьте консоль браузера** (F12) - там будут подробные логи
2. **Проверьте админ-панель** - заказы должны сохраняться даже если Telegram не работает
3. **Проверьте Bot Token и Chat ID** - самая частая причина проблем
4. **Отправьте тестовое сообщение** из админки для диагностики

Все заявки сохраняются в localStorage даже если Telegram не работает!
